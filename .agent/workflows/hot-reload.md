---
description: Hot reload the running Flutter app
---

1.  **Send Hot Reload**: The agent will send 'r' to the running flutter process.

// turbo
2.  **Execute**: (Agent: Use `send_command_input` with `Input='r'` on the active command ID).
